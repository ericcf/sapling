- content_for :personal do
  - if session[:user]
    %p
      = 'user: ' + User.find(session[:user]).username
      |
      = link_to 'log out', user_logout_url
  - else
    %p= link_to 'log in', user_login_form_url